export default interface EventDetail {
  id: string;
  event_no: string;
  event_name: string;
  event_date: string;
  event_time: string;
  event_fee: string;
  event_description: string;
  is_registered: number;
}
